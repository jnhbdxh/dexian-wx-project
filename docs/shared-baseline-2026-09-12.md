# 共同代码基线收口记录

日期：2026-09-12（Asia/Shanghai）

## 基线定义

- 收口来源为 `main` 的提交 `82c83457960fc47846b218c5bef5d0ae951d4c19` 加当时工作区内已经交付的管理端、小程序、共享 API、迁移、测试、文档和正式证据。
- 最终共同基线以 Git 标记 `shared-baseline-2026-09-12` 指向的完整提交为唯一依据；后续开发不得用旧 HEAD、局部文件哈希或未提交工作区代替。
- 迁移链固定为 `0000` 至 `0009_next_colonel_america`。阶段 2B-1 的 `0010`、`0011` 不属于本基线，须在基线确认后分配，并分别进入两个独立发布版本。
- 当前主开发任务承担唯一整合责任，包括共享 Schema、journal、路由、锁文件、最终回归和交付登记。

## 纳入与排除

- 纳入已复核的预约、确认、顾客预约历史、后台接待查询、资源日历、请假、预约政策、支付／核验接线、公共后台示例及相应测试和正式证据。
- `docs/evidence/` 保存正式引用的截图与 JSON；原 `.impeccable/` 审查缓存、临时日志、私有配置、测试数据库、构建和覆盖率产物不进入版本管理。
- 历史计划和决策只追加当前完成状态，不删除当时事实；真实账号、真机网络、完整跨端链路和未完成的浏览器视觉检查继续单独登记。

## 可复现验证

整合负责人从候选提交创建独立干净检出，执行 `pnpm install --frozen-lockfile`；在独立 PostgreSQL 数据库从零应用全部迁移，然后执行三端测试、全仓类型检查、全仓生产构建、Prettier 检查、Git 差异检查和本地文档链接检查。

验证完成后才创建共同基线标记。标记说明和交付记录登记被测完整提交 SHA、Git tree、执行时间、测试结果、迁移终点以及明确未执行的真实环境验收项。
